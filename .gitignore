# Secrets — never commit. Copy .env.example → .env and add your key.
.env

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Runtime data (SQLite DB + user config + logs) — not source
data/
*.log

# PyInstaller / flet pack build output
build/
dist/
*.spec

# Editor / tooling
.vscode/

# Claude Code local state, worktrees, and machine-local settings
.claude/
